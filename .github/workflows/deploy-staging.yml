name: Deploy Staging

on:
  push:
    branches:
      - develop
  
jobs:
  deploy:
    uses: leanazulyoro/test_gh_actions/.github/workflows/reusable-deploy.yml@develop
    with:
      environment: staging


      